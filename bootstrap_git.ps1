# Bootstrap Discrepancy Desk docs repo
# Run from inside C:\dev\x\discrepancy-desk-docs after extracting this pack.

git init
git branch -M main
git status
git add .
git commit -m "Initialize Discrepancy Desk planning docs"

Write-Host "Docs repo initialized and committed."
Write-Host "Next: create GitHub repo discrepancy-desk-docs and add remote origin."
